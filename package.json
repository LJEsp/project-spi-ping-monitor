{
  "name": "project-spi-ping-monitor",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ping.js": "^0.2.1",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-scripts": "2.0.5",
    "web-pingjs": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "homepage": "https://ljesp.github.io/project-spi-ping-monitor",
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
